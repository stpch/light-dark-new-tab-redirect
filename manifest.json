{
    "manifest_version": 3,
    "name": "New Tab Color Redirect",
    "version": "1.0.0",
    "permissions": [
        "storage"
    ],
    "description": "Redirects new tabs to a custom URL with a configurable background color (light, dark, device or custom).",
    "chrome_url_overrides": {
        "newtab": "redirect.html"
    }
}
